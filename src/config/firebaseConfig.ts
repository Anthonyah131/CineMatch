/**
 * Firebase Configuration
 * CineMatch App
 */

// El firebase-config.js se debe descargar del Firebase Console
// y colocar en android/app/google-services.json

// Esta configuración se inicializa automáticamente con @react-native-firebase/app
export const firebaseConfig = {
  // La configuración se maneja automáticamente por 
  // @react-native-firebase/app cuando google-services.json
  // está presente en android/app/
};

export default firebaseConfig;